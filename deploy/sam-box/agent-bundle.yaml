# Current source nests identity and egress. This credential-free sample cannot start
# an identified sandbox; a platform-issued credential and issuer/audience are required.
version: v1alpha1
agent:
  id: a0-default
  external_id: a0-default
  credential: ""
egress:
  allow: []
