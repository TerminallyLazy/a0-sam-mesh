[tool.pytest.ini_options]
markers = [
    "integration: requires an Agent Zero checkout with the plugin installed",
]

[tool.ruff]
line-length = 100
extend-exclude = ["docs/superpowers"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
